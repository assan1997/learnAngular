import { CustomAtributeDirectiveDirective } from './custom-atribute-directive.directive';

describe('CustomAtributeDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomAtributeDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
